## TODO

#### Preview image should be skipable back and forward
wwhen user doesn't want to move an image in thee preview user should be able to skip that photo or return back to previously skipped images.

#### Multiple Select destination folders
when user wants to add multiple folders in a directory, user currently has to select them one by one. But instead user should be able to select multiple folders.

#### Option to save destination folders as sets
When user exits the app and come back later to continue, they have to add the folders they were working on previously. but instead they should be able to save and give a name for the current list of destionation folders. and delete when they wish. also if they add new destionation folders it should be added to the saved set.

#### Zoom in to the preview image
When a previewed image is a image containing text. it's hard to read the content so users should be able to zoom in and out by pinching trackpad or scrool wheel
